// ============================================================================
// order_repository.dart — ชั้นที่เขียนออเดอร์ลงฐานข้อมูล   ★ มีจุดที่ต้องเติม
//
// คู่กับ menu_repository.dart — ไฟล์นั้นอ่าน ไฟล์นี้เขียน
// จอตะกร้าเรียกใช้ฟังก์ชันของไฟล์นี้ และไม่เรียกฐานข้อมูลตรง ๆ
//
// ── ส่วนที่ให้มาแล้ว ────────────────────────────────────────────────────────
//   - OrderRepository.instance   ตัวเดียวที่ทั้งแอปใช้ร่วมกัน
//   - จอตะกร้าที่เรียกใช้ placeOrder() ครบแล้ว (lib/screens/cart_screen.dart)
//     จอรวบรวมรายการในตะกร้าเป็นออเดอร์หนึ่งใบและส่งรหัสประจำตัวผู้ใช้ (uid)
//     มาให้พร้อมแล้ว
//
// ── ส่วนที่คุณต้องเติม (มองหาคำว่า "จุดที่ต้องเติม") ──────────────────────────
//   - placeOrder()   งานข้อ ⑤
// ============================================================================
import '../models/food_order.dart';

class OrderRepository {
  // ตัวสร้างแบบส่วนตัว — สร้างวัตถุของคลาสนี้จากไฟล์อื่นไม่ได้ ให้ใช้ instance ข้างล่าง
  OrderRepository._();

  /// ตัวเดียวที่ทั้งแอปใช้ร่วมกัน เรียกด้วย OrderRepository.instance.placeOrder(...)
  static final OrderRepository instance = OrderRepository._();

  // ── ★ จุดที่ต้องเติม (งานข้อ ⑤) — เขียนเอกสารออเดอร์ลง Cloud Firestore ──────
  // ต้องทำ: เขียนเอกสารออเดอร์หนึ่งเอกสารลง Cloud Firestore จริง
  //   เอกสารนั้นต้องมีฟิลด์ที่เก็บรหัสประจำตัวผู้ใช้ (uid) ของบัญชีที่กำลัง
  //   เข้าสู่ระบบอยู่ ซึ่งส่งเข้ามาทางพารามิเตอร์ uid ข้างล่างนี้
  //   ชื่อฟิลด์และฟิลด์อื่นในเอกสาร คุณเป็นผู้ตัดสินเอง
  //   ผลที่ต้องได้: กดสั่งหนึ่งครั้งขณะเข้าสู่ระบบอยู่ แล้วเห็นเอกสารหนึ่งเอกสาร
  //   เกิดขึ้นในหน้าคอนโซลของ Cloud Firestore พร้อมค่า uid ที่ตรงกับบัญชีนั้น
  //
  // ★ ค่าที่ order.toMap() คืนออกมาเป็นเพียงจุดตั้งต้น ปรับได้ตามที่คุณตัดสินไว้
  //   (ดู lib/models/food_order.dart)
  //
  // ถ้าเขียนไม่สำเร็จ ให้โยนข้อผิดพลาดออกไปตามเดิม จอตะกร้าจับไปแสดงเอง
  Future<void> placeOrder({
    required FoodOrder order,
    required String uid,
  }) async {
    throw UnimplementedError('ยังไม่ได้ทำ — งานข้อ ⑤ ของแล็บ');
  }
}
