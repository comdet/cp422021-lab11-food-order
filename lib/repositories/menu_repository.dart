// ============================================================================
// menu_repository.dart — ชั้นที่ไปเอารายการเมนูมาให้จอเมนู   ★ มีจุดที่ต้องเติม
//
// repository คือที่รวมโค้ดที่ติดต่อแหล่งข้อมูลไว้ที่เดียว จอเรียกใช้ฟังก์ชันของ
// repository แล้วได้รายการกลับไป โดยจอไม่ต้องรู้ว่ารายการนั้นมาจากที่ใด
//
// ── ส่วนที่ให้มาแล้ว ────────────────────────────────────────────────────────
//   - MenuRepository.instance   ตัวเดียวที่ทั้งแอปใช้ร่วมกัน
//   - loadMenu()                ตอนนี้คืนรายการจาก demo_menu.dart
//
// ── ส่วนที่คุณต้องเติม (มองหาคำว่า "จุดที่ต้องเติม") ──────────────────────────
//   - loadMenu()   งานข้อ ①
// ============================================================================
import '../data/demo_menu.dart';
import '../models/menu_item.dart';

class MenuRepository {
  // ตัวสร้างแบบส่วนตัว — สร้างวัตถุของคลาสนี้จากไฟล์อื่นไม่ได้ ให้ใช้ instance ข้างล่าง
  MenuRepository._();

  /// ตัวเดียวที่ทั้งแอปใช้ร่วมกัน เรียกด้วย MenuRepository.instance.loadMenu()
  static final MenuRepository instance = MenuRepository._();

  // ── ★ จุดที่ต้องเติม (งานข้อ ①) — อ่านรายการเมนูจาก Cloud Firestore ────────
  // ต้องทำ: เปลี่ยนให้ฟังก์ชันนี้คืนรายการเมนูที่อ่านมาจาก Cloud Firestore ของ
  //   Firebase project ที่คุณสร้างเอง แทนรายการตัวอย่างที่เขียนค้างไว้ในโค้ด
  //   ผลที่ต้องได้: แก้ชื่อหรือราคาของเมนูรายการหนึ่งในหน้าคอนโซลของ Firebase
  //   แล้วเปิดจอเมนูอีกครั้ง สิ่งที่ขึ้นบนจอต้องเปลี่ยนตามค่าในฐานข้อมูล
  //
  // ★ ฟังก์ชันนี้คือที่เดียวที่จอเมนูใช้ดึงข้อมูล — จอไม่รู้จักแหล่งข้อมูล
  //   เปลี่ยนที่นี่ที่เดียวพอ ไม่ต้องแก้ไฟล์ในโฟลเดอร์ screens และ widgets
  //
  // ข้อมูลที่อ่านจากฐานข้อมูลมาในรูป Map — ใช้ MenuItem.fromMap() แปลงเป็น
  // MenuItem ได้ทันที (ดู lib/models/menu_item.dart)
  Future<List<MenuItem>> loadMenu() async {
    return demoMenu;
  }
}
